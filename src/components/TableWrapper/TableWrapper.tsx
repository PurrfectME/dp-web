import './TableWrapper.scss';

export const TableWrapper = ({children}: any) => (
    <div className="table-wrapper">
        <div className="beb">
            {children}
        </div>
    </div>
);

